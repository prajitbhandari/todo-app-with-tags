alert('The item id is :' + <%= @todo.id %>);

$('#delete<%= @todo.id %>').parent().html(
    '<%= link_to "Recover", recover_todo_path(@todo), method: :put, id: "recover#{@todo.id}", remote: true %>'
)
    .append('&nbsp;<%= link_to "Purge", purge_todo_path(@todo), method: :delete, data: { confirm: 'Are you sure?' } , id: "purge#{@todo.id}", remote: true %>'
    );




